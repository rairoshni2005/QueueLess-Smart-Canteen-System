{
  "name": "queueless-root",
  "version": "1.0.0",
  "description": "Smart Campus Food & Queue Management System",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",

    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm run dev --prefix frontend",

    "dev-ai": "cd ai && uvicorn app:app --reload --host 127.0.0.1 --port 8000",

    "dev": "concurrently \"npm run dev-backend\" \"npm run dev-frontend\" \"npm run dev-ai\"",

    "seed": "node backend/src/seed.js",

    "start-backend": "npm start --prefix backend"
  },

  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}